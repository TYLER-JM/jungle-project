<tr>
  <td>
    <h4><%= category.name %></h4>
  </td>
  <td>
    <%= category.id %>
  </td>
  <td>
    <%= category.created_at.strftime("%B %d, %Y") %>
  </td>
  <td>
    <%= category.updated_at.strftime("%B %d, %Y") %>
  </td>
  <td>
    <%= Category.find(category.id).products.count %>
  </td>
</tr>